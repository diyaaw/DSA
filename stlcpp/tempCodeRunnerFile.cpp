//difference of syntax () {} between emplace and push_back
// vector<pair<int,int>>vec;
// vec.push_back({1,2});
// vec.emplace_back(1,2);

// //iteraotr points at the memory address where your element is
// vector<int> :: iterator it=vec.begin();
// cout<<*it;// this works as a pointer and gives us data at
// //the location of it

// vector<int> :: iterator itt=v.rend();
// cout<<*it1;// not that imp
// vector<int> :: iterator ittt=v.rbegin();
// cout<<*it2;
// //generally emplace is fadter than vector

// cout<<vec[0];
